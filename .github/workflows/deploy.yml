name: Deploy NotionVoxBot to AWS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: eu-west-1
  TERRAFORM_VERSION: 1.6.0

jobs:
  deploy:
    name: Deploy to AWS
    runs-on: ubuntu-latest
    
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Build and push Docker image to ECR
        run: |
          # Get AWS account ID
          AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          ECR_REPOSITORY="notionvox"
          IMAGE_TAG="latest"
          
          # Login to ECR
          aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY
          
          # Build Docker image
          docker build -t $ECR_REPOSITORY:$IMAGE_TAG .
          
          # Tag image for ECR
          docker tag $ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          
          # Push image to ECR
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          
          echo "Docker image pushed successfully to ECR"

      - name: Terraform Init
        run: terraform init
        working-directory: terraform

      - name: Import existing AWS resources (ignore errors)
        continue-on-error: true
        run: |
          # Set variables for import commands
          export TF_VAR_telegram_bot_token="${{ secrets.TELEGRAM_BOT_TOKEN }}"
          export TF_VAR_openai_api_key="${{ secrets.OPENAI_API_KEY }}"
          export TF_VAR_notion_token="${{ secrets.NOTION_TOKEN }}"
          export TF_VAR_notion_database_id="${{ secrets.NOTION_DATABASE_ID }}"
          export TF_VAR_aws_region="${{ env.AWS_REGION }}"
          
          # Import existing IAM role
          terraform import aws_iam_role.lambda_role notionvoxbot-lambda-role || echo "IAM role not found or already imported"
          
          # Import existing CloudWatch log group  
          terraform import aws_cloudwatch_log_group.lambda_logs /aws/lambda/notionvoxbot-prod || echo "Log group not found or already imported"
          
          # Import existing Lambda function if it exists
          terraform import aws_lambda_function.notionvoxbot notionvoxbot-prod || echo "Lambda function not found or already imported"
          
          # Import existing Lambda function URL if it exists
          terraform import aws_lambda_function_url.notionvoxbot_url notionvoxbot-prod || echo "Lambda function URL not found or already imported"
          
          # Import existing Lambda permission if it exists
          terraform import aws_lambda_permission.allow_function_url notionvoxbot-prod/AllowExecutionFromFunctionURL || echo "Lambda permission not found or already imported"
        working-directory: terraform

      - name: Terraform Plan
        run: |
          terraform plan \
            -var="telegram_bot_token=${{ secrets.TELEGRAM_BOT_TOKEN }}" \
            -var="openai_api_key=${{ secrets.OPENAI_API_KEY }}" \
            -var="notion_token=${{ secrets.NOTION_TOKEN }}" \
            -var="notion_database_id=${{ secrets.NOTION_DATABASE_ID }}" \
            -var="aws_region=${{ env.AWS_REGION }}" \
            -out=tfplan
        working-directory: terraform

      - name: Terraform Apply (on main branch only)
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: terraform apply tfplan
        working-directory: terraform

      - name: Get Lambda Function URL
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: |
          FUNCTION_URL=$(terraform output -raw lambda_function_url)
          echo "Lambda Function URL: $FUNCTION_URL"
          echo "LAMBDA_FUNCTION_URL=$FUNCTION_URL" >> $GITHUB_ENV
        working-directory: terraform

      - name: Set Telegram Webhook (on main branch only)
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: |
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/setWebhook" \
            -H "Content-Type: application/json" \
            -d '{"url": "'${{ env.LAMBDA_FUNCTION_URL }}'"}'

      - name: Test Lambda Function
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: |
          curl -X GET "${{ env.LAMBDA_FUNCTION_URL }}" || true

      - name: Cleanup (on failure)
        if: failure() && github.ref == 'refs/heads/main'
        run: |
          terraform destroy -auto-approve \
            -var="telegram_bot_token=${{ secrets.TELEGRAM_BOT_TOKEN }}" \
            -var="openai_api_key=${{ secrets.OPENAI_API_KEY }}" \
            -var="notion_token=${{ secrets.NOTION_TOKEN }}" \
            -var="notion_database_id=${{ secrets.NOTION_DATABASE_ID }}" \
            -var="aws_region=${{ env.AWS_REGION }}" || true
        working-directory: terraform
